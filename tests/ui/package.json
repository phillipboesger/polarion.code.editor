{
  "name": "polarion-ui-tests",
  "version": "1.0.0",
  "description": "Playwright UI tests for the Polarion Code Editor plugin",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
