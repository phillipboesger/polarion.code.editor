<?xml version="1.0" encoding="UTF-8"?>
<!--
  Jakarta variant of the web deployment descriptor (Polarion 2606 / Tomcat 11).

  Polarion 2606 ships Apache Tomcat 11 (Jakarta EE 11 / Servlet 6.1), which only
  accepts descriptors in the https://jakarta.ee/xml/ns/jakartaee namespace.
  Keep this file in sync with src/main/webapp/WEB-INF/web.xml (the javax /
  Servlet 3.x variant used for Polarion 2512 / Tomcat 9); the only intended
  differences are the descriptor namespace/version and the Servlet API package.
-->
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_1.xsd"
	version="6.1">

	<display-name>CodeEditor</display-name>

	<servlet-mapping>
		<servlet-name>default</servlet-name>
		<url-pattern>/</url-pattern>
	</servlet-mapping>

	<welcome-file-list>
		<welcome-file>index.html</welcome-file>
	</welcome-file-list>

	<servlet>
		<servlet-name>polarion-code-editor</servlet-name>
		<servlet-class>boesger.polarion.codeeditor.api.CodeEditorServlet</servlet-class>
		<load-on-startup>0</load-on-startup>
	</servlet>

	<servlet-mapping>
		<servlet-name>polarion-code-editor</servlet-name>
		<url-pattern>/api/*</url-pattern>
	</servlet-mapping>

	<filter>
		<filter-name>DoAsFilter</filter-name>
		<filter-class>com.polarion.portal.tomcat.servlets.DoAsFilter</filter-class>
	</filter>
	<filter-mapping>
		<filter-name>DoAsFilter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>

	<security-constraint>
		<web-resource-collection>
			<web-resource-name>All</web-resource-name>
			<url-pattern>/*</url-pattern>
		</web-resource-collection>
	</security-constraint>

	<login-config>
		<auth-method>BASIC</auth-method>
		<realm-name>PolarionRealm</realm-name>
	</login-config>

</web-app>
